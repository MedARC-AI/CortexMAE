# intermediate temporal patch size
t_patch_size: 4

masking: tube

model_kwargs:
  no_decode_pos: true
  no_embed_class: true
  head_init_scale: 0.0

datasets:
  hcp-train:
    url: /data/fmri-datasets/pretrain/hcpya-task.${input_space}.wds/hcpya-task-${input_space}-{00000..00896}.tar
    buffer_size: 2000

plot_period: 5
